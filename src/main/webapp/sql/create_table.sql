create table i_user(
	id int auto_increment primary key,
	user_name varchar(30), 
	user_password varchar(20), 
	name varchar(50),
	gender varchar(5),
	grade int,
	tel_no varchar(20),
	lottery_flag char(1)
	);